apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-pods
spec: 
  replicas: 1
  selector:
    matchLabels: 
      app: nginx2
  template:
    metadata:
      labels:
        app: nginx2
    spec: 
      containers:
        - name: backend-trial-pod
          image: nginx:latest